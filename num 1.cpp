#include <iostream>
#include <math.h>
#include <iomanip>

using namespace std;

int main()
{
int x,y;
cin >> x >> y;
cout << setprecision(4) << ( (double(1 + sin(x + y)sin(x+y))) / (2 + abs(x - 2 * x / (1 + xx * y*y)))) + x;
return 0;
}
